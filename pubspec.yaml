name: sih_new
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Widened for better compatibility

dependencies:
  flutter:
    sdk: flutter

  # Firebase Packages (versions will be resolved automatically)
  firebase_core:
  firebase_auth:
  cloud_firestore:

  # Other Packages
  cupertino_icons: ^1.0.8
  permission_handler: ^11.4.0
  flutter_reactive_ble: ^5.4.0
  flutter_ble_peripheral: ^1.2.6 # Note: You had 1.2.6, ensure this is correct
  uuid: ^4.5.1
  device_info_plus: ^11.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true